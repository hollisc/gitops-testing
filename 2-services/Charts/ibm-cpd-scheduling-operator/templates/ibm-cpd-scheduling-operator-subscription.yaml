apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: {{ .Values.subscriptions.ibmcp4d.name }}
  annotations:
spec:
  {{- toYaml .Values.subscriptions.ibmcp4d.subscription | nindent 2 }}