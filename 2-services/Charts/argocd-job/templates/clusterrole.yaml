kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: {{ .Values.name }}
  annotations:
    argocd.argoproj.io/hook: {{ .Values.hooktype }}  
rules:
  - verbs:
      - get
    apiGroups:
      - config.openshift.io
    resources:
      - ingresses